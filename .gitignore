correspondence/

# macOS
.DS_Store

# Secrets — defensive belt-and-suspenders. This is a PUBLIC Jekyll repo.
# Never drop credentials here. These globs catch accidents.
.env
.env.*
*.p8
*service-account*.json
*-credentials.json
*.keystore

# Jekyll build artifacts
_site/
.bundle/
vendor/bundle/
.jekyll-cache/
.jekyll-metadata

# Resolved server-side by Pages. A lock generated on an older Ruby pins below
# production, so it is not tracked — see the note in Gemfile.
Gemfile.lock
